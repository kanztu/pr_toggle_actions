on:
- pull_request

git: false
trivy: false
file: false
docker: false
project: false
terraform: false
publish: false

raw:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v2
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
